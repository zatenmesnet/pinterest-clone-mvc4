@model IEnumerable<TTModels.Posts>
@using TTModels
@{
    ViewBag.Title = "Profile " + @ViewBag.ProfileName;
}

@section profile{
    <h2 class="pname">Profile @ViewBag.ProfileName</h2>
}

@foreach (Posts p in Model)
{
    @Html.Partial("Post", p);
}